[pytest]
addopts = -q --cov=apps/backend/backend --cov-report=term-missing --cov-fail-under=60
testpaths = tests
markers =
    firestore_integration: Firestore Emulator-backed persistence and read-back coverage

