# Build and Release Folders
bin-debug/
bin-release/
[Oo]bj/
[Bb]in/

# Other files and folders
.settings/

# Executables
*.swf
*.air
*.ipa
*.apk

# Project files, i.e. `.project`, `.actionScriptProperties` and `.flexProperties`
# should NOT be excluded as they contain compiler settings and other important
# information for Eclipse / Flash Builder.

.env
.env.local
.env.*
!.env.example
!.env.ci
.envrc
.direnv/
# E2E 用ローカル環境変数（テスト用トークンなどを含み得るため除外）
tests/e2e/.env
tests/e2e/.env.*
# UI テスト用ローカル環境変数（トークン/認証情報が混入し得るため除外）
tests/ui/.env
tests/ui/.env.*
# 認証トークンを含む可能性のあるパッケージマネージャ設定
.npmrc
.npmrc.local
.pnpmrc
.pnpmrc.local
.yarnrc
.yarnrc.yml
.yarnrc.local
# ローカル仮想環境や Firebase デバッグログは資格情報を含みうるため除外
.venv/
.firebase/
firestore-emulator-data/
firebase-debug.log
firebase-debug.*.log
firestore-emulator-*.log
firestore-debug.log
# Firestore シードやエミュレータのエクスポートはサンプルとはいえ利用者入力が含まれ得るため除外
firestore-seed-*/
firestore-seed-*.json
firestore-export-*/
firestore-export-*.json
# Cloud Run デプロイ用の秘密情報は必ずローカルファイル (.env.deploy など) に閉じ込める
.env.deploy
.env.deploy.*
!env.deploy.example
.cloudrun.env.*
# Cloud Build の一時生成 YAML は誤コミット防止のため除外
.cloudbuild.backend.*.yaml
configs/cloud-run/*.env.local
configs/cloud-run/*.secrets
apps/backend/.env
apps/backend/.env.*
google-oauth-client-secret.json
client_secret_*.json
firestore-service-account*.json
gcp-service-account*.json
*.pem
*.p12
*.pfx
*.key
__pycache__/
*.pyc
# Node
node_modules/
dist/
apps/frontend/.env
apps/frontend/.env.local
apps/frontend/.env.*
!apps/frontend/.env.example
# MSW のローカル生成物（開発時のみ生成される可能性があるため除外）
.msw/

# Python
__pycache__/

# Test artifacts
.coverage
.coverage.*
.pytest_cache/
coverage.xml
htmlcov/
coverage/

# Playwright artifacts (HTML report, traces, screenshots)
playwright-report/
test-results/
# a11y 検証のローカル出力（レポートを生成する場合に備える）
axe-report/
axe-results/
# Playwright 認証ストレージはセッション情報を含むため除外
tests/e2e/.auth/
tests/e2e/storage-state*.json

# Runtime diagnostics logs (may contain telemetry/PII)
*.log

# App runtime data
.data/
# Demo database snapshots (may contain sample user content)
.data_demo/

.cache/

*.sqlite3
*.db

# ローカルの gcloud 設定ディレクトリ（資格情報を含むため必ず除外）
.config/gcloud/
# ローカルの AWS 資格情報ディレクトリも同様に機密情報を含むため除外
.aws/

# Vector DB (Chroma) persistence - do not commit generated index data
.chroma/

.vscode/
# ローカルの秘密情報ディレクトリは誤コミット防止のため除外
.secrets/

# Plugin Eval runtime artifacts may contain usage, transcripts, and workspace output
**/.plugin-eval/runs/
**/.plugin-eval/benchmark-usage.jsonl
**/.plugin-eval/*.result.json
**/.plugin-eval/*.html

終了済みor参考ドキュメント/

**/.vite/
gcp-deploy-key.json
# google-github-actions/auth がworkspaceに作る短期credential fileを誤commitしない
gha-creds-*.json
