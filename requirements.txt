fastapi>=0.136,<1.0
uvicorn
structlog
pydantic-settings
pytest
httpx
pytest-cov
google-auth
# Firestore backend を安定運用するため v2 系へ明示固定
google-cloud-firestore>=2.28.0,<3.0.0
itsdangerous
langgraph
chromadb
cmudict
g2p_en
openai>=2.46.0
sentry-sdk
# 短期復旧: 既存の観測コードは v2 系 API（trace/span）に合わせているため固定
# v3 対応へ移行
langfuse>=3.1,<4
